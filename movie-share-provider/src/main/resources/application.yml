server:
  port: 8002
spring:
  application:
    name: movie-share-provider
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url:  jdbc:mysql://123.57.83.115:3306/movie_share?serverTimezone=Asia/Shanghai&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

swagger:
  enable: true